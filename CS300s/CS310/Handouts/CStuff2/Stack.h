/* push(n) pushes n onto the top of the stack. */
void push(int);
/* pop() returns the value on top of the stack
 * and, then, removes that top elemeont on the
 * stack. */
int pop();
/* isEmpty() returns true (1) if the stack is empty
 * and false (0) if the stack is not empty. */
int isEmpty();
