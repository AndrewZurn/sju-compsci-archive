/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
package csci230radomskizurnhardy;

import java.util.*;
/**
 * Class AdminController will be used to served the basic functions of the administrator for the
 * "Choose My College" software package being constructed in CS230 of the Spring of 2012.  The 
 * intended purpose of this class is to allow for the modifying of information important to the User and
 * University class also included in the csci230radomskizurnhardy package.
 * @author awzurn
 * @version 1.0
 * @since March 12, 2012
 */
public class AdminController {
    /**
     * A variable of an instance of Admin
     */
    private Admin admin;
    //private DatabaseController databaseController;
    
    /**
     * The constructor that will create the controller class that will be used by the admin and will
     * call upon the database controller class methods to change the information in the database.
     * @param admin can be used to call upon administrator methods.
     */
    public AdminController(Admin admin/*, DatabaseController databaseController*/){
        this.admin = admin;
        //this.databaseController = databaseController;
    }
    
    /**
     * 
     * @param school
     * @param state
     * @param location
     * @param control
     * @param numberOfStudents
     * @param percentFemales
     * @param SATVerbal
     * @param SATMath
     * @param expenses
     * @param percentFinancialAid
     * @param numberOfApplicants
     * @param percentAdmitted
     * @param percentEnrolled
     * @param academicsScale
     * @param socialScale
     * @param qualityOfLifeScale 
     */
    public void addUniversity(String school, String state, String location, String control,
            int numberOfStudents, double percentFemales, double SATVerbal, double SATMath,
            double expenses, double percentFinancialAid, int numberOfApplicants,
            double percentAdmitted, double percentEnrolled, int academicsScale,
            int socialScale, int qualityOfLifeScale){
        
        /*databaseController.addUniversity(school, state, location, control,
            numberOfStudents, percentFemales, SATVerbal, SATMath,
            expenses, percentFinancialAid, numberOfApplicants,
            percentAdmitted, percentEnrolled, academicsScale,
            socialScale, qualityOfLifeScale);*/
    }
    
    /**
     * 
     * @param school
     * @param emphasis 
     */
    public void addUniversityEmphasis(String school, String emphasis){
        
        //databaseController.addUniversityEmphasis(school, emphasis);
    }
    
    /**
     * 
     * @param firstName
     * @param lastName
     * @param username
     * @param password
     * @param type 
     */
    public void addUser(String firstName, String lastName, String username, String password, char type){
        //databaseController.addUser(firstName, lastName, username, password, type);
    }
    
    /**
     * 
     * @param Id
     * @param school
     * @return 
     */
    public boolean addUserSchool(int Id, String school){
        //boolean reply = databaseController.addUserSchool(Id, school);
        return true; //make sure to fix return statement!
    }
    
    /**
     * 
     * @param Id 
     */
    public void deactivateUser(int Id){
        //databaseController.deactivateUser(Id);
    }
    
    /**
     * 
     * @param username 
     */
    public void deactivateUser(String username){
        //databaseController.deactivateUser(username);
    }
    
    /**
     * 
     * @param school
     * @param state
     * @param location
     * @param control
     * @param numberOfStudents
     * @param percentFemales
     * @param SATVerbal
     * @param SATMath
     * @param expenses
     * @param percentFinancialAid
     * @param numberOfApplicants
     * @param percentAdmitted
     * @param percentEnrolled
     * @param academicsScale
     * @param socialScale
     * @param qualityOfLifeScale 
     */
    public void editUniversity(String school, String state, String location, String control,
            int numberOfStudents, double percentFemales, double SATVerbal, double SATMath,
            double expenses, double percentFinancialAid, int numberOfApplicants,
            double percentAdmitted, double percentEnrolled, int academicsScale,
            int socialScale, int qualityOfLifeScale){
        
        /*databaseController.editUniversity(school, state, location, control,
            numberOfStudents, percentFemales, SATVerbal, SATMath,
            expenses, percentFinancialAid, numberOfApplicants,
            percentAdmitted, percentEnrolled, academicsScale,
            socialScale, qualityOfLifeScale);*/
    }
    
    /**
     * 
     * @param school
     * @param emphases 
     */
    public void editUniversityEmphases(String school, ArrayList<String> emphases){
        //databaseController.editUniversityEmphases(school, emphases);
    }
    
    /**
     * 
     * @param Id
     * @param firstName
     * @param lastName
     * @param password 
     */
    public void editUser(int Id, String firstName, String lastName, String password){
        //databaseController.editUser(Id, firstName, lastName, password);
    }
    
    /**
     * 
     * @param Id
     * @param firstName
     * @param lastName
     * @param password 
     */
    public void editUserByAdmin(int Id, String firstName, String lastName, String password){
        //databaseController.editUserByAdmin(Id, firstName, lastName, password);
    }
    
    /**
     * 
     * @return 
     */
    public TreeMap getAllPossibleEmphases(){
        //TreeMap emphasesMap = databaseController.getAllPossibleEmphases();
        return null;
    }
    
    /**
     * 
     * @return 
     */
    public TreeMap getAllSavedSchools(){
        //TreeMap savedSchoolMap = databaseController.getAllSavedSchools();
        return null;
    }
    
    /**
     * 
     * @return 
     */
    public TreeMap getAllUniversities(){
        //TreeMap universitiesMap = databaseController.getAllUniversities();
        return null;
    }
    
    /**
     * 
     * @return 
     */
    public TreeMap getAllUniversityNamesWithEmphases(){
        //TreeMap universitiesEmphasesMap = databaseController.getAllUniversityNamesWithEmphases();
        return null;
    }
    
    /**
     * 
     * @return 
     */
    public TreeMap getAllUniversityRecordsWithEmphases(){
        //TreeMap universityRecordEmphasesMap = databaseController.getAllUniversityRecordsWithEmphases();
        return null;
    }
    
    /**
     * 
     * @return 
     */
    public TreeMap getAllUsers(){
        //TreeMap userMap = databaseController.getAllUsers();
        return null;
    }
    
    /**
     * 
     * @param Id
     * @return 
     */
    public TreeMap getSpecificUser(int Id){
        //TreeMap userMap = databaseController.getSpecificUser(Id);
        return null;
    }
    
    /**
     * 
     * @param username
     * @return 
     */
    public TreeMap getSpecificUser(String username){
        //TreeMap userMap = databaseController.getSpecificUser(username);
        return null;
    }
    
    /**
     * 
     * @param args 
     */
    public static void main(String args[]){
        Admin admin = new Admin("AdminFirstName", "AdminLastName", "AdminUserName","password",'a');
        //DatabaseController databaseControllerTest = new DatabaseController();
        //new AdminController(admin, databaseControllerTest);
    }
}
